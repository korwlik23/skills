# --- Local Claude Code settings (machine-local, must never be pushed) ---
.claude/settings.local.json
.claude/*.local.json

# --- Third-party repos cloned in for reference only (have their own .git) ---
# obra/superpowers.git — external MIT project, not part of this repo
/superpowers/
# jirateep12z/skills.git — a different person's repo, not part of this repo
/v3/
# jirateep12z/skills.git (newer iteration) — has its own nested .git; not part of this repo
/v4/

# --- OS / editor noise ---
.DS_Store
Thumbs.db
